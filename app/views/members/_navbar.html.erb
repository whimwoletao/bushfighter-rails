<nav class="navbar navbar-default">
            <div class="container-fluid">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar bar1"></span>
                        <span class="icon-bar bar2"></span>
                        <span class="icon-bar bar3"></span>
                    </button>
                    <% if @team.nil? %>
                       <%= image_tag('bh.jpg', style:"height: 90px;width: 720px;" )%>
                    <% end %>

                </div>
                <div class="collapse navbar-collapse">

                    <ul class="nav navbar-nav navbar-right">



                        <% if !@team.nil? %>
                        <li>
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                <p><%= @team.name %></p>
                                <i class="ti-settings"></i>
                                <b class="caret"></b>
                            </a>
                            <ul class="dropdown-menu">
                                <li>
                                   <%= link_to edit_team_path(params[:team_id ] || params[:id ] ) do %> Settings <% end %>
                                </li>
                            </ul>
                        </li>
                        <% end %>

                        <li>
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                            <!-- this should  dynamic give member name -->
                                 <p>Welcome!</p> 
                                 <i class="ti-user"></i>
                                <b class="caret"></b>
                            </a>
                            <ul class="dropdown-menu">
                               <li><%= link_to "Dashboard", root_path %></li>
                               <li> <%= link_to "Account", member_account_path %> </li>
                               <li><%= link_to "Sign Out", destroy_member_session_path, method: :delete %></li>
                            </ul>
                        </li>
                    </ul>

                </div>
            </div>
        </nav>